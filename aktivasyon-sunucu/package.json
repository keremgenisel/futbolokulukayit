{
  "name": "futbol-okulu-aktivasyon",
  "private": true,
  "type": "module",
  "description": "Futbol Okulu Kayıt Programı lisans aktivasyon sunucusu (Cloudflare Worker + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute futbol-okulu-lisans --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute futbol-okulu-lisans --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
